# Web API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Config & validation (Pydantic v2)
pydantic==2.9.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# Core numerics
numpy==1.26.4
packaging==24.1

# Hugging Face (generator + local embeddings)
huggingface-hub==0.25.2

# Payments
stripe>=10.0.0,<12.0.0

# OpenAI SDK (new-style client, works with GPT-4.x / GPT-3.5 etc.)
openai>=1.40.0,<2.0.0
starlette>=0.37.2

# HTTP client (agent layer → OpenRouter)
httpx>=0.27.0,<1.0.0

# Postgres + pgvector (vector search + all business data)
asyncpg>=0.29.0,<1.0.0
